import { DashboardLayout } from "@/components/shared/layouts/dashboard-layout"; import { mockFreelancer } from "@/lib/mock-data"; export default function FreelancerLayout({ children, }: { children: React.ReactNode }) { const freelancerData = mockFreelancer; return ( <DashboardLayout userRole="freelancer" userName={freelancerData.name} userAvatar={freelancerData.avatar} > {children} </DashboardLayout> ); }
